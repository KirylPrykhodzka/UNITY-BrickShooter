using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterMovement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //detect wasd input. set movement direction to null if there is no input or 0-360 based on pressed buttons
    }

    void FixedUpdate()
    {
        //if movement direction != null move in the specified direction
    }
}
